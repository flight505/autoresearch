{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "_comment": "Check for autoresearch config and inject hardware summary. Re-injects on resume/clear/compact so hardware awareness survives compaction.",
        "hooks": [
          {
            "type": "command",
            "command": "'${CLAUDE_PLUGIN_ROOT}/hooks/run-hook.cmd' session-start"
          }
        ]
      }
    ]
  }
}
